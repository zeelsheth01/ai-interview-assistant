async def upload_resume(file):

    # later:
    # save file
    # parse text
    # store in database

    return {
        "message": "Resume uploaded successfully",
        "filename": file.filename
    }
